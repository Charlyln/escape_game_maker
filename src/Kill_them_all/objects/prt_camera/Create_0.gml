// global.scale = 4;
 
// window_set_size(480 * global.scale, 270 * global.scale);

//camera_set_view_size(view_camera[0], 480 * global.scale, 270 * global.scale);

//globalvar room_w, room_h;
//room_w = display_get_width() / room_width;
//room_h = display_get_height() / room_height;